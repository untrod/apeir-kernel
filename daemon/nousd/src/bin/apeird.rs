// APEIR canonical daemon entrypoint.
//
// The implementation remains shared with the historical `nousd` binary during
// the compatibility window so both names have exactly the same behavior.
include!("../main.rs");
