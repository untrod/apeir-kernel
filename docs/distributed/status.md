# Distributed runtime status

The repository contains node identity, node transport, placement data, and
fencing foundations. It does not implement consensus, a replicated journal, or
production ownership transfer between nodes. Multi-node execution is therefore
experimental. NKI remains loopback-only until authenticated remote transport is
implemented and reviewed.
